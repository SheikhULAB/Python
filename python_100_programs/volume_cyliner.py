import math

radius = float(input("Enter the radius of the cylinder: "))
height = float(input("Enter the height of the cyliner: "))

volume = math.pi * radius * radius * height

print("Volume of the cylinder: ", volume)